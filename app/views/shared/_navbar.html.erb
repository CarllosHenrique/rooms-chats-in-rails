<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <a class="navbar-brand" href="<%= root_path %>">NeTHUB</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="<%= root_path %>">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= rooms_path %>">Rooms</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= users_path %>">Users</a>
        </li>
      </ul>
      <% if user_signed_in? %>
        <a class="nav-link" href="<%= profile_path current_user.id %>"><%= current_user.name %></a>
      <% else %>
        <a class="nav-link" href="<%= new_user_session_path %>">Register | Login</a>
      <% end %>
    </div>
  </div>
</nav>
<br>